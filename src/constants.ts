export const WIDTH = 600;
export const HEIGHT = 200;
// * Для более плавной графики.

export const DPI_RATIO = 2;
// * для координат (data) размерность canvas: 1200 x 400, а сам элемент 600 х 200
export const DPI_WIDTH = WIDTH * DPI_RATIO;
export const DPI_HEIGHT = HEIGHT * DPI_RATIO;

export const ROWS_COUNT = 5;
export const PADDING = 40;

export const VIEW_HEIGHT = DPI_HEIGHT - PADDING * 2;
export const VIEW_WIDTH = DPI_WIDTH;

export const SPEED = 3000;
